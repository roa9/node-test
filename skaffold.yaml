apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/roa9/node-test:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/roa9/node-test:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: node-test
        chartPath: charts/node-test
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/roa9/node-test'
          image.tag: '{{.DIGEST_HEX}}'
